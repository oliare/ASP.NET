@model CategoryCreateViewModel

<div class="container col-md-4 mt-4 ">
    <h2 class="text-center">Adding a new category</h2>

    <form asp-action="Create" method="post" class="mt-4" enctype="multipart/form-data">
        <div class="form-group">
            <label asp-for="Name"></label>
            <input asp-for="Name" class="form-control" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>
        <div class="form-group mt-2">
            <label asp-for="Image"></label>
            <input asp-for="Image" class="form-control" />
            <span asp-validation-for="Image" class="text-danger"></span>
        </div>
        @* <div class="form-group mt-2 row d-flex align-items-center">
            <label asp-for="Image" class="col-form-label col-md-12 mb-2"></label>
            <div class="col-md-4">
                <img id="preview" src="/images/default_img.jpg" style="max-width: 100%; display: block;" alt="Image" />
            </div>
            <div class="col-md-8">
                <input asp-for="Image" type="file" class="form-control" accept="image/*" onchange="filePreview(event)" />
                <span asp-validation-for="Image" class="text-danger"></span>
            </div>
        </div> *@

        <a href="/" class="btn btn-dark mt-4">Cancel</a>
        <button type="submit" class="btn btn-dark mt-4">Create</button>
    </form>

</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
   @*  <script>
        let filePreview = e => {
            var reader = new FileReader();
            var image = document.getElementById('preview');

            reader.onload = () => {
                image.src = reader.result;
                image.style.display = 'block';
                image.style.maxHeight = image.style.maxWidth = '120px';
            }
            reader.readAsDataURL(event.target.files[0]);
        }
    </script> *@
}
